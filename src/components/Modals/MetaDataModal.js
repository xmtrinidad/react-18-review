import './MetaDataModal.css'

const MetaDataModal = () => {
  return (
    <dialog open>
      <p>Greetings, one and all!</p>
      <form method="dialog">
        <button>OK</button>
      </form>
    </dialog>
  );
};

export default MetaDataModal;